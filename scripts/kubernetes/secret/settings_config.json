// kubectl create secret generic soboon-credentials --from-file=soboon-credentials=settings_config.json
{
  "aws": {
    "access_key_id": "키 입력",
    "secret_access_key": "키 입력",
    "s3_bucket_name": "soboon-static",
    "region": "ap-northeast-2"
  }
}
